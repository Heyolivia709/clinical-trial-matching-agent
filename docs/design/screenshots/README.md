# Screenshots

PNG exports, so the design renders inline on GitHub without opening the HTML files, and so the interactive behaviours have a record.

Expected files, exported from the design tool:

| File | Shows |
| --- | --- |
| `06-criterion-detail-at-rest.png` | the collapsed state a reader lands on |
| `06-criterion-detail-expanded.png` | propositions, tool calls, and evidence expanded |
| `07-verifier-attempt-1.png` | rejected citation, before correction |
| `07-verifier-attempt-2.png` | accepted citation, after one correction |
| `08-baseline-comparison.png` | full system beside both one-shot controls |
| `prototype-tool-stepping.png` | tool-call sequence partially revealed |
| `prototype-citation-drawer.png` | a citation resolved to its FHIR JSON path |

Capture at the 1180 px document width. Prefer the at-rest state as the primary image for any section that has one — it is what a reviewer sees first.

The at-rest and attempt-1 captures depend on gaps 2, 3, and 7 in [../README.md](../README.md) being addressed first.
